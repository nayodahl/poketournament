{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('create_form') }}
{% endblock %}

{% block title %}Grand Tournoi Pokémon{% endblock %}

{% block body %}

        <section>
            <div class="container section-inner">
                <div class="row">
                    <div class="col-lg-10 mx-auto">
                        {{ form_start(gameForm) }}
                            {{ form_row(gameForm.scorePlayer1) }}
                            {{ form_row(gameForm.scorePlayer2) }}
                        {{ form_end(gameForm) }} 
                            
                        <a class="btn btn-primary" href="{{ path('app_view') }}">Annuler</a>
                    </div>            
                </div>
            </div>
        </section>    

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('create_form') }}
{% endblock %}
